{
  "type": "object",
  "properties": {
    "apiBaseUrl": {
      "type": "string",
      "description": "Base URL exacta del API, incluyendo /api/v1."
    },
    "webAppOrigin": {
      "type": "string",
      "description": "Origin exacto autorizado para preparar sesiones de la extensión."
    },
    "nativeHostName": {
      "type": "string",
      "description": "Nombre registrado del host Native Messaging de Agency OS.",
      "default": "com.agencyos.helper"
    }
  }
}
