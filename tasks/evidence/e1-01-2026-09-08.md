# E1-01 — SEC-11 · 2026-09-08

Estado: corrección implementada en el árbol de trabajo; pendiente de commit para cerrar la tarea
formalmente.

## Resultado

- `JwtAuthGuard` captura únicamente errores de verificación del JWT.
- La consulta de usuario activo queda fuera del `try`; un fallo de PostgreSQL llega al filtro global
  como `500 INTERNAL_ERROR` y no se registra como `auth.token.denied`.
- Usuario inactivo o con rol obsoleto continúa devolviendo `401` y queda auditado con
  `denyReason = INACTIVE_USER`.
- `ApiClient` conserva el token frente a 500 o error de red durante refresh/reintento y lo limpia
  ante un 401 definitivo.

## Verificación ejecutada

| Comando | Resultado |
|---|---|
| `pnpm --dir backend exec vitest run src/common/auth/guards.spec.ts --pool=threads --maxWorkers=1 --minWorkers=1` | 22/22 |
| `pnpm --dir web-app exec vitest run src/services/api-client.test.ts --pool=threads --maxWorkers=1 --minWorkers=1` | 6/6 |
| `node --test backend/test/architecture-rules.test.mjs` | 7/7 |
| `pnpm typecheck` | PASS |
| `pnpm lint` | PASS |

Regresiones añadidas: fallo de consulta de BD sin auditoría falsa, 500 tras reintento, caída de
red durante refresh y 401 definitivo después de refresh.
